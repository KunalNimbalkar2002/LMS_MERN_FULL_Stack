import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
  name: "users", //----to edit the database as per required check     <<----------
  initialState: {},
  reducers: {},
});
